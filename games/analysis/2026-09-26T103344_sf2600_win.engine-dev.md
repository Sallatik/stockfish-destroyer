# Engine-dev: 2026-09-26T103344_sf2600_win
Destroyer 0.7.0 (engine-v6, has killer/history), white vs SF2600, win by mate, 79 plies.

**Mistake:** ply 53 (27.Rad1), best Kh1, -169cp (548->379). Still crushing; not a real risk.

**Pattern check:** the 4th flagged "Rad1" mistake overall, but not the same bug recurring: the first 3 (092327/092850/093510, all pre-killer/history, all ply 29, same opening structure) were a missed bishop redeploy (best Bc1/Bxf5/Bf4). This one is an unrelated king-safety/prophylaxis miss with 22/32 pieces still on the board. Coincidence of move name, not one root cause.

**Diagnosis:** Black has Qg5+h4+Rh8 lined up; Kh1 sidesteps the coming ...hxg3 opening the h-file before it happens. `eval.rs`'s own MG_KING PST scores g1 at +24 vs h1 at +14 — our static eval *prefers* staying on g1, so only calculation can override it, and without PVS/LMR yet this position is likely still ~8-9 ply deep. Killer/history only reorders moves for faster pruning; it can't fix an eval that points the wrong way, which is exactly why this recurred after round 7.

**Ranked items:**
1. King-safety v2 (queue item "king safety v2", `eval::evaluate` in engine/src/eval.rs): add an open-h-file-next-to-king term; use `4rk1r/b4p2/P1Pp4/1B1Pp1q1/7p/P1B2QP1/5P1P/R4RK1 w - - 1 27` (expect Kh1 >= Rad1) as a regression FEN. Est. **+15-30 Elo**. Test: gauntlet @0.1-0.2s vs current build + this FEN as a unit test.
2. PVS+LMR+aspiration (queue item "PVS+LMR+aspiration"): more depth also finds this, but it's the less targeted lever for this specific pattern.

No reorder from this game alone — it strengthens the king-safety-v2 regression set (note: per current JOURNAL, null-move already landed in round 8/engine-v7, after these games; the queue is now endgame-eval-pass -> PVS+LMR -> check-extensions -> king-safety-v2 -> small-fixes).
