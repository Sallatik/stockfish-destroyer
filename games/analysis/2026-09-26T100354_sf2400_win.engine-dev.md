# Engine-dev: 2026-09-26T100354_sf2400_win (engine-v5 "Destroyer 0.6.0", white vs SF2400, win by mate, 71 plies)

| ply | our move | best (SF d18) | eval loss | cause |
|---|---|---|---|---|
| 35 | Nxb7 | d4 | 280 cp | shallow depth + eval blind spot |

**Diagnosis:** only flagged mistake; position was ~equal (-7) beforehand, +287 for Black right after (mostly given back next move when Black answered with the weaker Qb8 instead of SF's own suggested Qc8, a -215cp mistake of its own). Replayed the FEN (`r2qr1k1/1pb2ppp/p1n5/2N4b/7P/1P1PBN2/1PP2PP1/R2Q1RK1 w - - 3 18`) on `bin/destroyer-engine-v5`: reaches depth 8/5s and returns `c5b7` (matches the game); its own score for Nxb7 *falls* as depth increases (167cp at d1 -> 139cp at d8) but never flips to `d4` at any reachable depth. So the search partially senses the downside but can't see far enough to prefer `d4`, compounded by `eval::evaluate()` (PeSTO material+PST only — no mobility/central-space/king-safety term) having no way to rate a quiet consolidating `d4` above an immediate pawn grab even if searched deeper.

**Ranked items:**
1. Null-move + LMR/aspiration in `Search::negamax`/`best_move` (queued) — same 7-8 ply ceiling confirmed in the other 2 games from this batch; would likely both punish Nxb7 harder and let d4 surface. Est. **+50-80 Elo**. Test: A/B gauntlet vs `bin/destroyer-engine-v5`.
2. *(new, small — fold into the queued pawn-structure/eval pass, don't split out)* add a cheap mobility term (legal-move-count difference) to `eval::evaluate()` in `engine/src/eval.rs` so quiet developing/central moves aren't dominated by 1-pawn grabs. Est. **+15-30 Elo**. Test: same gauntlet; extend the `central_pawn_beats_edge_pawn`-style unit tests.

Reorder note: like the other two SF2400 wins in this batch, this argues for null-move/LMR ahead of another king-safety-v2 attempt.
