# Engine-dev analysis: 2026-09-26T100752_sf2200_loss (Destroyer 0.6.0 / engine-v5+TT, commit 1d80be6, as White vs SF 2200)

## Blunder table (>=100cp drops)
| Move | Ours | Best | Loss(cp) | Likely cause |
|---|---|---|---|---|
| 20 | Ng7 | g3 | -136 | shallow depth / no SEE: unsound N sac, ...Bxg7 wins material |
| 23 | Nd5+ | Be2 | -136 | shallow depth / no SEE: unsound N sac for pawn+check |
| 34 | Rc3 | Kg2 | -100 | shallow depth / no killers: active-looking rook outranks quiet Kg2 |
| 35 | Ree3 | Rf3 | -124 | eval blind spot: no rook-activity/open-file term to prefer Rf3 |
| 45 | Kd1 | d6+ | -142 | missing check-extension: forcing, non-capture d6+ not seen |
| 73 | Bb5 | Ba6 | -401 | horizon effect in an already-hopeless (-1134) endgame, low practical impact |
| 79 | Be2 | Ba6 | -295 | same pattern (-969), low practical impact |
| 82 | Kxg4 | Be2 | -8757 | **depth cliff** (confirmed by replay below): mate only found at d13 |
| 84 | Bd7+ | Kh6 | -8675 | same depth cliff; threw back a momentary SF blunder-reprieve |

## Diagnosis (costliest causes)
1. **Cumulative positional drift, moves 10-15** (each under the 100cp threshold: 10.Re1 51cp, 11.Be3 53cp, 12.Rad1 47cp, 13.a3 30cp, 15.h5 45cp): took White from +19 to -100 before the first named blunder (move 20). No king safety, no pawn structure, no deeper (LMR/PVS) search in quiet positions — the single largest total eval loss of the game.
2. **Depth cliff in forced-mate-adjacent king/pawn-race endgames** (moves 82, 84): already lost either way, but the search's inability to reliably reach depth 13 in time turns "clearly lost" into "immediately mated" (see replay below).
3. **Discrete unsound sacs / missed forcing moves** (20, 23, 45): no SEE to reject bad captures, no check-extension for forcing pawn pushes.

## Focus checks
1. **Repetition/50-move** (python-chess replay of the full game): max halfmove clock = 14 (ply 162), never close to 100. Only 2-fold repeats occurred (plies 111/115 & 113/117 king shuffle, broken by ...Ne4+; 153/157; 167-173 Bd7+/Bc6 shuffle). `can_claim_threefold_repetition()` and `can_claim_fifty_moves()` were `False` at every single ply — no draw was ever legally available, so none was declined; we were also never ahead, so "repeated while winning" doesn't apply. `draw_score`/`is_repetition`/CONTEMPT logic is not implicated in this loss.
2. **Endgame replay**: fed the ply-82 FEN `8/8/8/1B2k3/4n1pK/4p2p/8/8 w - - 14 82` to `bin/destroyer-engine-v5` (matches commit 1d80be6, pre-killer-move build) at movetime 5000. Depths 1-11 all play Kxg4, score worsening -331 -> -1282; depth 12 (12.36M nodes, a **~15.6x** jump from depth 11's 792K) still plays Kxg4 at -1609; only depth 13 (29.7M nodes, using nearly the whole 5s budget) switches to Bd3 at -2071, and movetime<=4000ms reproducibly stops at depth 12/Kxg4. Static eval is not the blind spot here (it already flags -331 at depth 1); this is a raw depth/node-count problem in a near-capture-free position — move ordering (killers) and search efficiency (LMR/PVS) are the levers, not eval terms.
3. **TT across 190 plies**: in `negamax`, `pos.halfmoves()>=100 || is_insufficient_material() || is_repetition()` is checked *before* the TT probe/store, and draw returns never call `tt.store` — a repetition on one path can't poison a transposition on another path to the same key, or vice versa. Mate-score rebasing (`to_tt`/`from_tt`) round-trips correctly per existing unit tests and matches the replay's mate-score progression. No TT-caused bug found in this game; 2M entries clearing only on `ucinewgame` is a capacity/staleness question, not a correctness one, and out of scope here.

## Ranked next steps (reordering the existing queue given this game's evidence; max 3)
1. **PVS + LMR + aspiration windows** — promote above king-safety-v2: the ~15.6x node blowup at depth 12 and the depth-13-or-bust finding above show raw search depth, not eval terms, decided moves 82/84, and LMR/PVS would also cut the moves-10-15 positional drift by searching quiet positions deeper for the same 5s. Est. **+80-150 Elo**. Test: `arena.gauntlet bin/destroyer bin/destroyer-<tag>` at 0.2-1s, plus rerun the ply-82 FEN above expecting a non-Kxg4 move well before the 5s cap.
2. **Killer/history ordering** (already testing) — reaffirmed, not reordered: this game's build had zero killer moves, and its worst blunder sits in a near-capture-free endgame, exactly where killers matter most. Finish validating and merge before LMR, since LMR's reductions need trustworthy move ordering to stay safe. Test: as already planned (self-play gauntlet vs current `bin/destroyer`).
3. **Pawn structure (passed pawns + mop-up)** — promote above king-safety-v2: the decisive endgame is a pure connected-passed-pawn race the eval can't see at all, while king safety v2 has almost no relevance to this particular game (queens left the board at move 7, neither king ever castled). Est. **+20-40 Elo**, plus fewer of these "-8000cp" horizon losses in similar pawn races. Test: synthetic K+P vs K/KB endgame suite (already planned) + gauntlet.

## Risks
- LMR/check-extensions must keep honoring the wall-clock `deadline` (checked every 2048 nodes) inside reduced/extended branches — verify with full-game timing logs, not just gauntlet Elo, given the 15.6x node variance seen between adjacent depths here.
- Any extension must respect `MAX_PLY` (96) the way `quiesce` already guards `ply >= MAX_PLY`, or `killers[ply]`/`path` indexing panics mid-game (UCI crash risk), not just a slow move.
- Re-run `cargo test` (existing repetition/50-move/mate-distance/TT tests) after each change, before `scripts/promote.sh`.
