# Engine-dev: 2026-09-26T103212_sf2500_win
Destroyer 0.7.0 (engine-v6, has killer/history), white vs SF2500, win by mate, 101 plies.

**Mistake:** ply 85 (43.Bd4), best Rh7+, -489cp (1312->823). Position is already 2R+B+6P vs R+B+2P with Black's king on f7; still wins, just slower.

**Diagnosis:** Rh7+ is forcing — verified Black has only 2 legal replies (Kg8/Kf8) — exactly the near-free case check extensions exist for. We instead played a quiet centralizing Bd4, which let Black interpose ...Bb6 hitting it for a free tempo (that tempo is the eval gap). `order_score` (engine/src/main.rs) gives no bonus for giving check, and the move loop in `negamax` applies no check extension, so a 2-ply-forced line scores no higher than an ordinary quiet move at the same nominal depth. This is the second game in this batch hitting the identical gap (also 103949's Ra6+/Kxe1, analyzed separately).

**Ranked items:**
1. Check extensions + qsearch fixes (queue item "check extensions", `Search::negamax` move loop in engine/src/main.rs): extend depth by 1 (capped) when `child.is_check()`; add `r2b3R/1p3k2/6p1/8/P1P1RP2/1PB5/6PP/6K1 w - - 3 43` (expect Rh7+ >= Bd4) to its regression set. Est. **+15-30 Elo**. Test: replay the FEN at fixed depth before/after, then gauntlet vs current build.
2. PVS+LMR+aspiration (queue item "PVS+LMR+aspiration"): raises depth generally; secondary to item 1 for forcing lines specifically.

**Reorder suggestion:** 2 of these 4 games (plus 103949 separately, 3 total this round) hit the identical missed-forcing-move gap. Worth swapping "check extensions" ahead of "PVS+LMR+aspiration" in the current queue — cheap, targeted, now 3x corroborated.
