# engine-dev analysis — 2026-09-26T091300_sf2000_win (Destroyer 0.4.0 vs SF 2000, white win by mate)

## Blunder table
No move ≥100cp drop for either side. Largest inaccuracies (informational only, all still winning):
| # | our move | best | loss cp | cause |
|---|---|---|---|---|
| 23 (c4) | c4 | Qxa6 | 80 | shallow/no-TT: many winning moves look equal at low depth, picks a slower consolidating one |
| 20 (Nxd5) | Nxd5 | Ng4 | 60 | move ordering/no TT — didn't find the marginally-better fork/pin |
| 21 (Nf6+) | Nf6+ | Nf6+ | 41 (still found the best move) | none — SF's reply Qxf6 was itself forced/near-forced, eval swing is from SF's position, not our search |
| 4 (c3) | c3 | O-O | 33 | opening-book gap (already queued fix) |

## 21.Nf6+ / 23.c4-c5xd6 — was it deep search or a gift?
It was a gift. Ply 40 SF played 20...Bd6 (best was exd5); eval swung from -104 to **-553** purely from SF's own error — SF hung the fork. Our 21.Nf6+ is a one-move knight fork with check (K must move or Qxf6 forced) — a shallow, tactically obvious shot that qsearch/depth-2 finds trivially; no evidence this required deep search. The subsequent 23.c4/24.c5/25.cxd6 pawn-storm is pure mop-up in an already +6..+9 pawn position (24.c5 and 25.cxd6 both matched Stockfish's own best move), not a found combination. **This game does not demonstrate deep tactical search; it demonstrates correctly not fumbling a winning position after the opponent blundered.**

## Time management
Fixed movetime path (arena always sends `movetime`, not `wtime`), budget = 4900ms after the 100ms safety margin. No stalls, no depth collapses, mate found cleanly in the final forced sequence. No time-management issues to report in this game.

## New, concrete source observation
We cannot verify what depth/nodes were actually reached on ply 21/39/45 — `best_move()` (main.rs) only `println!`s `info depth {depth} score cp {alpha} nodes {…}` to stdout per completed iteration; the arena/analysis pipeline does not capture this per-move, so evals.json has no depth/node column. This is exactly the "depth logging" item already in the queue — this game is a concrete argument to **move it to the top of the "small fixes" batch** (near-zero effort, unblocks future engine-dev analyses like this one from having to guess "deep or shallow?").

## Queue re-ordering
No change to relative order of TT+hash-move / check-extensions / killer-history / null-move / PVS+LMR. This game had zero blunders, so it gives no new evidence about which weakness costs the most Elo — it's a clean data point, not a diagnostic one. One addition: pull **depth/node-per-move logging** (already listed under "small fixes") forward, ahead of contempt-hysteresis and wtime-cap, since it's cheap and directly improves the value of every future engine-dev report.
