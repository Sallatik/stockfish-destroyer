# Engine-Dev Analysis — 2026-09-26T094633_sf2400_loss (vs SF 2400, loss by checkmate)

Engine: Destroyer 0.6.0 = engine-v5 (TT + hash-move ordering only; killer/history is NOT in this build — commit be61978-dirty). We are white.

## Blunder table

| Move (ply) | Our move | Best (SF) | Loss (cp) | Likely cause |
|---|---|---|---|---|
| 12.Ke2 (23) | Ke2 | Kd1 | 80 | eval blind spot: no king-safety term penalizes giving up castling into an open board |
| 16.Kd1 (31) | Kd1 | Rf1 | 180 | shallow depth / horizon during a forced check-reply chain |
| 17.Qd5 (33) | Qd5 | Qd3 | 123 | shallow depth, no SEE on the queen contact |
| 18.Qb3 (35) | Qb3 | Qc4 | 256 | shallow depth, missed a 2–3 ply tactic off ...Nb4 |
| 22.Ke3 (43) | Ke3 | Ke3 | 135 | SF depth-18 instability, not a real alternative (already forced-lost) |
| 33.Ka3 (65) | Ka3 | Rf1 | 8894 | shallow depth (see below) — walks into a forced mate |

## Ply 65 deep dive

- Full-strength `bin/stockfish` on the FEN before Ka3, depth 87: **after Ka3, Black mates in 6** (`score mate 6`, PV `Qc2 Rb1 Qxb1 Kb3 ...` — matches the game's actual finish move for move).
- The position was already forced-mate-lost *before* Ka3: the true best try `Rc1` only survives to **mate in 8** (needs SF depth ≥20 to see; the depth-18 pass behind evals.json still read "only" −1096 cp and named `Rf1`). Ka3 cost ~2 tempi of resistance, not an otherwise-holdable game.
- Replayed the exact FEN with `bin/destroyer-engine-v5`, `movetime 5000`: reaches **only depth 8** (14.5M nodes) and returns **`b3a3` (Ka3)** at cp −992 — reproduces the game move verbatim. 14.5M nodes for 8 ply is a poor yield: move ordering, not time management, is the bottleneck.

## Diagnosis (ranked by cost)

1. **Move-ordering-limited shallow depth** (no killer/history in this build): directly confirmed at ply 65 (depth 8 hides the mate-in-6). The same signature likely explains ply 31/33/35.
2. **No check extensions**: ply 23→46 is a 20+ ply forced-reply chase (Ke2→Kd1→...→Ke3→Kd3→Kc3→Kd4) where the engine repeatedly plays the locally-least-bad reply and only discovers the trap 2-3 ply later (−180, −123, −256). This stretch is the actual game-losing one; ply 65 is just the final symptom.
3. **No king-safety eval term**: 12.Ke2 (−80cp, the inciting move) surrenders castling with no compensating penalty in `eval.rs::evaluate`.

## Queue impact

Doesn't reorder the queue. **Killer/history** (item 1, already testing) is what would most directly have fixed ply 65 — deeper search in 5 s plausibly finds `Rf1`/`Rc1` instead of `Ka3` — but wouldn't have saved the *game*, already forced-lost around move 18-23. **Check extensions** (item 2) targets that actual root cause and should stay next after killer/history lands.

## Ranked code-level items (this game's evidence, max 3)

1. **Killer/history heuristic** — `engine/src/main.rs` `Search::order_score`/`record_cutoff` (already written, untested in this game). Evidence: depth 8 / 14.5M nodes at ply65's position. Est. **+60-100 Elo**. Test: gauntlet vs current build; add regression FEN = ply65 FEN, expect depth >8 and move ≠ Ka3 at 5 s.
2. **Check extensions, capped** (queue item 2) — `Search::negamax`: extend `depth` by 1 (cap ~8 cumulative per line) when `pos.is_check()` before recursing. Est. **+20-40 Elo**, mostly fewer catastrophic losses like this one. Test: FEN before ply23 (`r3k2r/pp3ppp/1qnPb3/8/2B1Q3/5N2/PPP2bPP/R1B1K2R w KQkq - 0 12`), expect the search to prefer `Kd1` over `Ke2` at equal time.
3. **King safety v2, shield + open files only** (queue item 3) — `engine/src/eval.rs::evaluate`. Est. **+15-30 Elo** (v1's squared attack term over-shot and lost 56 Elo; a linear/capped version should avoid that). Test: existing v1 gate FEN plus this game's ply23 FEN, and a short gauntlet before promoting.

## Risks

- Check extensions: cap total extensions per line and keep `ply < MAX_PLY` (96) — `self.killers[ply as usize]` will panic out of bounds otherwise; reuse the existing node-count deadline check so a long forcing line can't blow the 5 s/move budget.
- King-safety changes repeat v1's risk (shallow-depth over-weighting) — gate on the v1 regression FEN and a gauntlet, not just unit tests.
- None of these three touch `think_time`/UCI I/O, so the 5 s/move rule and UCI correctness aren't directly at risk, but confirm with a real `movetime`-bounded gauntlet before promoting, not `--time` shortcuts only.
