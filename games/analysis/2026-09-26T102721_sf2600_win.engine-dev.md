# Engine-dev: 2026-09-26T102721_sf2600_win
Destroyer 0.6.0 (engine-v5, no killer/history — JSON `our_engine` overrides the commit tag), white vs SF2600, win by mate, 95 plies. **0 flagged mistakes (>=100cp).**

**Near miss:** ply 33 (17.Rxe7), best Ne8, -93cp (350->257), just under the mistake threshold. Verified: Ne8 attacks Bf6, and if ...Rxe8 then Re1xe8+ wins the exchange with check (the e-file is already open); we instead just traded rooks evenly on e7. Same family as this batch's missed-forcing/tactical-fork misses (103212, 103515), just smaller and below `arena/evalgame.py`'s 100cp threshold.

**Diagnosis:** a clean, mistake-free win despite lacking killer/history entirely — evidence that killer/history (added round 7) is a search-speed aid, not what separates clean games from ones with blunders. Across this 4-game batch, the actual differentiator is whether a forcing 2-3 ply combination (fork, check, sac) gets valued correctly, which is a depth/eval question, not a move-ordering one.

**Ranked items:**
1. PVS+LMR+aspiration (queue item "PVS+LMR+aspiration"): the Ne8 fork needs ~2 extra ply of confirmed calculation to outrank the simple trade; general depth gains are the most direct lever for this near-miss class. Elo already tracked at queue level; not separately estimable from one near-miss.
2. Check extensions + qsearch fixes (queue item "check extensions"): the ...Rxe8/Rxe8+ pair is a forced recapture-check; would also let the search verify this exact line cheaply once found.

No reorder — supportive, not decisive, evidence that both items are already correctly queued ahead of king-safety-v2.
