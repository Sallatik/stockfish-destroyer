# Engine-dev: 2026-09-26T111000_sf2800_draw — Destroyer 0.8.0 (engine-v7) vs SF2800, draw by threefold repetition, 52 plies, 0 mistakes ≥100cp

**Final position** (White to move; claimable by the "next move repeats" rule after ply 52): `r3k3/1p3p2/3Q4/4p2p/Pn6/5q2/1PP2P1P/R1B2RK1 w q - 9 27` — White Q+2R+B+5P vs Black Q+R+N+4P, **up a full rook**, king boxed on g1/h1 by alternating Qf3+/Qg4+. Checked every White reply from ply 45-51 (`Kh1`/`Kg1`): each one had **exactly one legal move**, no block or capture ever available. This is a 100% forced sequence, the result of Black's sound Rxg2+/Nxd6+ exchange sac starting at ply 42, not a choice our engine made.

**Contempt branch:** our pre-patch static eval at every one of those root calls is **+508..+552** (materially accurate — we really are up a rook), well above the -300 gate, so **`CONTEMPT`=30 (avoid-draws) was active throughout and never gated off.** It made no difference because there was nothing to choose between — the arbiter (`chess.Board.is_game_over(claim_draw=True)` in `arena/play.py`) auto-ends the game one move before the literal 3rd occurrence, so White never even got to move at the final position. Nothing was declined; SF2800 earned this perpetual outright.

**Contempt sizing:** irrelevant in this specific game (a forced, single-legal-move line — no contempt value, however large, changes it), but 30cp is undersized where it *does* matter: the companion SF2800 draw this round drifts through a minor-piece ending with our own eval floating +70..+120, where a flat 30cp penalty is noise-sized next to normal search fluctuation. Recommend scaling `CONTEMPT` (`main.rs`) with how far ahead the last searched root score says we are, e.g. `(root_score.clamp(0, 400) / 10).max(10)`, rather than opponent Elo (we don't know it in-game) — fold into the already-queued hysteresis fix, same commit.

**Ranked items:**
1. Not this game's fault, but the enabling condition is fixable: letting g2 become a lone, rook-attackable pawn next to our own king (moves ~14-21) is what handed Black the sac. 3rd independent case this round for king-safety-v2 (open-file/undefended-pawn-near-king term); no reorder, just added urgency. Est. **+30-50 Elo** (queue item, priority unchanged).
2. New, small: scale `CONTEMPT` with the searched root score instead of a flat constant (formula above), bundled into the queued contempt-hysteresis fix in `main.rs`'s `go` handler. Est. **+5-10 Elo** (draws count fully against us at the top level). Test: gauntlet A/B vs flat-30 baseline, plus check it doesn't destabilize move choice at 0.1-0.2s in the fast gauntlet regime.
