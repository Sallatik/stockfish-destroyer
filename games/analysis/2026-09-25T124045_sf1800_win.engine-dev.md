# Engine-dev analysis: 2026-09-25T124045_sf1800_win

## Blunder table
| Ply | Our move | Best | Eval loss | Cause |
|---|---|---|---|---|
| 39 | c4 | h4 | 134cp | Horizon effect: refuted by `...Nd4!` (fork on Qc2/Ne3), 4 plies deep. Full-width negamax + pure MVV-LVA ordering (`negamax`, `mvv_lva`) has no TT/killers, so at 5s the achievable depth doesn't cover the whole forced line to see it nets nothing. |
| 45 | Qc5+ | fxe3 | 240cp | Horizon + eval blind spot. `Qc5+` forces `Qd6/Qxd6+/Kxd6`, trading queens while leaving Black's e3 passer (2 squares from queening) uncaptured; `fxe3` was simply better. `eval.rs::evaluate` has no passed-pawn/threat term — a runaway passer is only worth whatever its PST square happens to score, not "must deal with this now." Combined with no check extension, the forcing look of `Qc5+` isn't searched any deeper than a quiet move, so its real cost (post-trade, bishop-pair-vs-passer swing to -320) is past the horizon. |

## Diagnosis
1. **Shallow effective depth from missing move-ordering infra** (no TT/hash move, no killers) — both blunders are concrete tactics/consequences a few plies past what plain alpha-beta+MVV-LVA reaches in 5s on a busy middlegame. This is the dominant cost.
2. **Eval blind spot on passed pawns** — `evaluate()` has material+PST only; an advanced, unopposed pawn (e3) isn't flagged as urgent, so quiet-looking captures/checks that ignore it look equal to the moves that stop it. Directly caused ply 45.
3. **No check extensions** compounds #1: forcing-looking moves (`Qc5+`) get the same nominal depth as quiet ones, so their long-term cost/benefit isn't resolved.

## Endgame conversion (53 moves)
Not evidence of a mop-up/passed-pawn bug: eval rose monotonically through the whole stretch (exchange win at ply53 → b-pawn promotion sac at ply92 → mate at ply105), halfmove clock never neared 50, no repetition risk. The length is just shallow search picking safe consolidating moves over the fastest technique. **No reorder needed** for the queued mop-up/king-safety item on this evidence.

## Ranked suggestions (not re-proposing queued TT/killers/null-move/PVS-LMR/check-ext/mop-up items)
1. **Pull a narrow "passed pawn" bonus out of the queued king-safety/passed-pawn item and land it first**, in `eval.rs::evaluate` (per-pawn: no enemy pawn on same/adjacent file ahead → rank-scaled bonus, e.g. +20/40/80/150 for ranks 4-7). Cheap, no search changes, directly fixes the ply45 blindspot. Est **+15-30 Elo**. Test: run `bin/destroyer` on the ply45 FEN and confirm `fxe3` now scores clearly above `Qc5+`; then gauntlet vs current build.
2. **Harden time management** (`main.rs::think_time`, node-check in `negamax`/`quiesce`): tighten the deadline poll from `nodes & 2047` to `nodes & 511` and raise the safety margin from 100ms to ~150-200ms. Guards against 5s overshoot under quiescence blowups or CPU contention from 4-8 concurrent campaign slots. Test: gauntlet at `--concurrency 6-8`, log max observed move time, assert <5000ms.
3. **Add UCI `stop` handling**: `main()`'s match only covers `uci/isready/ucinewgame/position/go/quit`; `stop` is silently dropped while blocked inside `best_move`. Spawn a stdin-reader thread setting an atomic flag checked alongside the deadline. Low Elo, but a UCI-correctness gap if the harness ever pre-empts a search (e.g. future `--abort-hopeless`). Test: pipe `position startpos\ngo movetime 5000\nstop` and confirm prompt `bestmove`.
4. **No reorder for TT/hash-move** — already first in queue; this game (ply 39 `...Nd4!` miss) is exactly the kind of tactic it should catch, reinforcing its priority as-is.
5. **No reorder for mop-up/king-safety** — keep at current position; the long conversion here was steady progress, not stalling (see above), except item 1's narrow slice.

## Risks
- 100ms time margin + 2048-node poll interval is the main 5s-rule fragility, worse once the campaign runs more concurrent slots (item 2).
- Missing `stop` handling is a latent UCI-correctness gap (item 3), currently masked because the engine self-limits to its own deadline.
