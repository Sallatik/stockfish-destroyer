# Engine-dev analysis: 2026-09-26T093447_sf2200_loss (Destroyer 0.5.0 / engine-v4 vs SF 2200, no TT)

## Blunder table
| Ply | Our move | Best (SF) | Eval loss | Likely cause |
|---|---|---|---|---|
| 31 | Ng5 | Be2 | -197cp | Shallow depth: no follow-up/threat; PST alone rates the square fine |
| 49 | cxb5 | Qxa6 | -134cp | Shallow depth: mechanical MVV-LVA capture beats the cleaner Qxa6 |
| 57 | Ne2 | a5 | -163cp | Eval blind spot: no pawn-structure/initiative term values the a5 break |
| 63 | Nc3 | b6 | -142cp | Eval blind spot: same, prefers a piece shuffle over the b6 break |
| 67 | gxh6 | b6 | -143cp | Eval blind spot: king safety — opens g/h files next to our own king |
| 71 | g3 | Kf1 | -9762cp, mate in 4 | Eval blind spot (primary) + shallow depth (see below) |

## Diagnosis (ranked by cp cost)
1. **No king safety at all** (`eval.rs::evaluate` is pure material+PST) — directly causes ply 67 and the fatal ply 71 g3, and let the kingside erode unnoticed from move 20 (hxg4) onward. One move (g3) accounts for >9700 of the ~10500cp total lost in the game.
2. **No move-ordering memory at game time** (no TT, no killers/history; flat MVV-LVA re-sorted every node in `negamax`) — full-width search, explains the four ~130-200cp misses (31/49/57/63) that are only a few plies of calculation away for SF.
3. **No pawn-structure/passed-pawn term** — 57/63/67 all favor a slow piece move over a concrete pawn lever (a5/b6).

## Focus checks
- **Mate length**: `bin/stockfish` on the post-g3 FEN finds **mate in 4 for Black** from depth 18 through depth 40 (PV `Rh7 Nh4 Rxh4 f3 Qh1+ Kf2 Rh2#`).
- **Does engine-v5 (TT) avoid it now?** No. `bin/destroyer` (v0.7.0, TT+hash-move) on the pre-g3 FEN `4n1k1/6r1/3p4/QPnPp3/P1P1Pp2/2N2N1q/5PP1/3RR1K1 w - - 0 36` at `go movetime 5000` reaches only **depth 7 / 1.5M nodes** and still plays **g2g3**, scoring it **cp +685** (true eval ~-234) — TT buys node efficiency, not the ~1-2 extra plies this mate needs, and doesn't touch the blind spot.
- **King-safety numbers**: `eval.rs` currently has an in-progress `king_safety()` (SHIELD_NEAR=14/FAR=7, OPEN_FILE_OWN=18/BOTH=12, attacker units² capped 15, queen=5/rook=3/minor=2). Replaying it on this FEN gives white `king_safety(after g3)=-34` vs `king_safety(after Kf1)=-36` — **the wrong sign**: the 1-square zone scan counts the black rook's ray stopping *on* our defended g2 pawn as "attacking the zone," penalizing Kf1 almost as much as g3 loses the shield outright. A file already occupied by an enemy R/Q needs its own term.

## Ranked code-level items (max 3)
1. **`engine/src/eval.rs::king_safety`: add "enemy major piece already on an own-pawnless file adjacent to the king"**, independent of the attack-unit zone scan: `ROOK_ON_OPEN_KING_FILE = 20`, `QUEEN_ON_OPEN_KING_FILE = 35` (mg-only, ~0.25-0.43 pawn @82cp). Here it turns g3's -34 into -69 while Kf1 stays -36 (33cp swing) — enough to flip two moves otherwise dead-equal under both king_safety and plain PST. Test: unit test on FEN `4n1k1/6r1/3p4/QPnPp3/P1P1Pp2/2N2N1q/5PP1/3RR1K1 w - - 0 36` asserting `king_safety(white, after Kf1) > king_safety(white, after g3) + 25`; then rerun the `go movetime 5000` check above and confirm bestmove != `g2g3`. ~30 min.
2. **Queue order unchanged — king safety stays #1**, gated on beating this FEN before merge: this game shows the already-shipped TT (engine-v5) cannot rescue this blunder class at depth 7/5s, and null-move/LMR (queued after) mostly buy node efficiency in quiet positions, not the extra plies a king-hunt tactic needs. Only a static term below full search depth is cheap enough to catch this before the deadline.
3. **Add a permanent regression test** using this game's critical FEN (`4n1k1/6r1/3p4/QPnPp3/P1P1Pp2/2N2N1q/5PP1/3RR1K1 w - - 0 36`) asserting a depth-6 search never returns `g2g3` once king safety lands — cheapest insurance (~15 min) against re-blundering the single costliest mistake (9762cp) found so far.

## Risks
- `king_safety()`'s extra per-leaf scan (loop over all pieces, sliding-attack lookups, twice per `evaluate` call) adds cost to every node; re-measure nps and reconfirm 5s/move compliance in `arena/play.py` after landing. If nps drops >15-20%, cache attacker units or skip the scan in qsearch stand-pat.
- Keep any new king-file terms MG-phase-only (tapered via the existing `mg[]`-only wiring), or endgame king activity gets wrongly penalized.
- Pure eval change: no UCI protocol or time-management surface touched, so no correctness/5s-rule risk expected.
