# Engine-dev: 2026-09-26T100831_sf2400_win (engine-v5 "Destroyer 0.6.0", white vs SF2400, win by mate, 59 plies)

| ply | our move | best (SF d18) | eval loss | cause |
|---|---|---|---|---|
| 49 | Qd5+ | Qxf5+ | 106 cp | shallow depth (horizon effect) |

**Diagnosis:** only flagged mistake, and cosmetic — already +11..+18, mates 5 moves later regardless. Replayed the exact FEN (`5r2/ppN2k2/7p/4Pp2/1B4nq/5Q1P/PPP2PP1/R4RK1 w - - 0 25`) on `bin/destroyer-engine-v5`: at 5000ms it reaches depth 8 (13.7M nodes) and returns `f3d5` (Qd5+, matches the game); capped at 250ms/depth 5 it returns `f3f5` (Qxf5+, SF's pick) instead. That's a genuine depth-dependent flip, not a move-ordering bug — both candidates are checks, and the root loop in `best_move()` searches every root move to full width at each depth (no pruning at the root), so ordering only affects speed, never the final choice. Root cause: our search plateaus at 7-8 ply at 5s/move on a mid-game position; deeper search would resolve the same forcing tactics Stockfish sees.

**Ranked items** (both already queued — this game is corroborating evidence, no reorder vs check-extensions, but does argue null-move/LMR should come before another king-safety attempt):
1. Null-move pruning in `Search::negamax` (`engine/src/main.rs`) — cheap, directly raises the depth ceiling this game hit; disable in-check/low-material to avoid zugzwang blindness. Est. **+50-80 Elo**. Test: A/B gauntlet vs current `bin/destroyer-engine-v5`.
2. Aspiration windows around the iterative loop in `Search::best_move` — narrower windows at low/mid depth free nodes for another ply. Est. **+15-25 Elo**. Test: compare nodes-to-depth-8 on saved middlegame FENs before/after.
