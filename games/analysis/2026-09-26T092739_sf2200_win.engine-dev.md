# Engine-dev report: 2026-09-26T092739_sf2200_win (white vs SF2200, win by mate, 0 mistakes ≥100cp)

## Book usage
Verified by probing `engine/book.txt` with the game's exact EPDs (board+side+castling+ep, matching
`Epd::from_position(pos, EnPassantMode::Legal)`): moves 1.e4 2.Nf3 3.Nxe5 4.Nf3 5.d4 (plies 1,3,5,7,9)
are all book hits (`e2e4`, `g1f3`, `f3e5`, `e5f3`, `d2d4`). Move 6.Bd3 (ply 11) is the first miss — this
is exactly the depth cap: `arena/book.py`'s `BRANCHING = [5, 4, 3, 3, 2]` has 5 entries, so `walk()`
stops recursing (`if level >= len(args.branching): return`) after white's 5th book move, regardless of
opponent replies. Every game will exit book by white move 6 at the latest.

## Book exit quality
Good. Eval at exit (after 5...d5, ply 10) was +52cp for us, and the engine's own search (not book) then
found the strong 8.Re1! (pinning the loose Ne4/Bf5 idea) and punished Black's 8...Bg6? (best was O-O;
drop 75cp) followed by 9...Bb4?? (drop 102cp) into a lost position by move 10. No book-exit regression.

## Book implementation notes
- EPD generation parity looks correct: python-chess `Board.epd()` defaults to `en_passant='legal'`,
  matching shakmaty's `EnPassantMode::Legal` — same semantics (ep field only set if the capture is
  actually legal, not just pseudo-legal). No mismatch observed in this game's lines.
- No bug found in `probe()`'s prefix-match scan for this game's positions.

## Ranked concrete items (book-focused)
1. **Deepen the book.** Extend `BRANCHING` in `arena/book.py` (e.g. `[5,4,4,3,3,2,2]`) and regenerate
   `engine/book.txt`. Cheapest lever available: this game shows our prep line is sound and already wins
   material by move 9-10 versus SF2200 mistakes; 2 more book plies keeps us on the prepared line through
   the critical 9...Bb4?? branch point instead of relying on fresh search there. Test: regenerate, rerun
   this exact opening against SF2000/2200/2400 gauntlet, confirm eval-at-exit parity or better.
2. **Add an EPD parity guard.** Add a small cross-check (Python script or `#[test]` fixture using known
   ep/pin edge cases, e.g. a pinned pawn where an en-passant capture is pseudo-legal but illegal) that
   asserts python-chess `board.epd()` and shakmaty's `Epd::from_position(.., EnPassantMode::Legal)`
   produce byte-identical strings. Currently unverified beyond common lines; a future shakmaty/python-chess
   version bump could silently desync ep-field legality rules and cause book misses with no error (probe
   just returns `None`, indistinguishable from "not in book").
3. **Low priority:** once `BRANCHING` grows, `book.rs::probe` stays a linear scan over `BOOK.lines()`
   (531 lines now); the file is written pre-sorted by `book.py` so a binary search (or build-time
   `phf` map) would be O(1)/O(log n) instead — not worth it until the book is meaningfully larger than
   ~1-2k lines given the 5s/move budget.
