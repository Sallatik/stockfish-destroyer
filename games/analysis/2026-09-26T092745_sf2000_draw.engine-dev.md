# engine-dev: 2026-09-26T092745_sf2000_draw (Destroyer 0.5.0 / engine-v4 vs SF 2000)

## Blunder table
| ply | our move | best | eval loss | cause |
|---|---|---|---|---|
| 73 | Rxf2 (capturing the checking N) | Kg1 | 117cp | eval blind spot: no king-safety/SEE term, so eval.rs scores R+N-for-Q-recapture (net: exchange for two pawns, exposed Kh1) as roughly fine. Kg1 keeps the whole rook (knight must retreat with tempo loss, e.g. Nd3), material +4 vs +2-with-black-compensation after Rxf2. This is a 1-ply-deep tactic our full-width negamax clearly reached (Qxf2 matches SF's forced reply exactly) — not a depth/horizon problem, purely a static-eval misjudgment of the resulting position. |

No other move in the game lost >20cp; this is the only tagged mistake.

## Repetition/draw diagnosis (plies 73-81)
Reconstructed: after 37.Rxf2 Qxf2 the true position (SF eval) is White down ~90-130cp (material: Q+N+4P=16 vs Q+B+2P=14 on paper, but Black's active Q+B vs an exposed Kh1 is worth ~3 pawns that our PST+material-only `evaluate()` doesn't model). `evaluate(&pos)` at the ply-73 root (before the blunder) is clearly > -300 (White still materially ahead there), so the `< -300` contempt gate (`main`, `go` handler) was **not** triggered — CONTEMPT=30 applied normally throughout, this is **not branch (a)**.

The repeated position (`.../1Q5K w - - 0 38` recurring at plies 75/79/final) is reached via plain checks (Qb8+/Kh7/Qb1+/Kh8), never inside a capture sequence, so `is_repetition` fired correctly in `negamax` at normal ply, not in `quiesce` — **not branch (b)**; the missing check-evasion-repetition-detection-in-qsearch bug is real elsewhere but is exonerated for this specific draw.

This is **branch (c)**: SF's own analysis lists our exact moves (Qb8+/Qb1+ shuffle) as its `best` at every one of these plies (drop_cp -3..+1), i.e. Stockfish agrees no non-repeating alternative was better for White either — the position really is a small, hard-to-avoid deficit after the blunder, and repeating for the draw-by-contempt (`draw_score` ≈ +30 for the side to move given the true ~-100cp reality) was the correct practical choice by our search, not a bug.

## Ranked items (does not change queue order; only #1 is newly motivated by this game)
1. **King safety terms in `eval.rs`** (queued: "king safety"). Add attacker-count/king-shield/open-file-near-king penalty so exchange sacs that expose the king (like Rxf2xf2) are scored correctly instead of "material-only fine." Est. **+40-60 Elo**. Test: gauntlet current vs candidate build (`arena.gauntlet`), plus a unit test replaying this exact FEN (`7h8/3b1Rp1/8/2p1P3/3N4/2P1q1P1/5n1P/1Q5K w`) asserting Kg1 ≥ Rxf2 in the eval-tagged search.
2. **Lightweight SEE for the move that gives up material in a check-response/capture** (fits the "small fixes" bucket already queued). Reject/deprioritize captures whose SEE is negative before search commits at low depth; cheap, catches this class of blunder even before king-safety eval lands. Est. **+15-25 Elo**, ~1-2h.
3. **TT + check extensions/qsearch fixes** — priority unchanged by this game (repetition here was correctly handled at negamax level; TT still helps depth/ordering generally, and the qsearch repetition gap remains a latent bug for a *future* game where the loop is inside a check-evasion capture chain, just not this one).
