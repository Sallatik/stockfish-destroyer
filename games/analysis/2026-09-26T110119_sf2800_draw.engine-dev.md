# Engine-dev: 2026-09-26T110119_sf2800_draw — Destroyer 0.8.0 (engine-v7) vs SF2800, draw by threefold repetition, 111 plies

| ply | our move | best (SF d18) | eval loss | cause |
|---|---|---|---|---|
| 33 | fxe3 | Rxe3 | 158cp (-100→-258) | recapture creates doubled e3/e4 pawns; engine-v7 has zero doubled-pawn penalty to prefer Rxe3 |
| 49 | Nc4 | Qb3 | 136cp (-72→-208) | shallow depth / no minor-piece-activity signal in a queen+minors middlegame |

**Diagnosis:** from ~ply 70 the game is a 2N-vs-2B(+pawns) ending; full-strength SF has White at -300..-400 throughout, but *our own* pre-patch static eval (PeSTO-only, no structure terms — what engine-v7 actually played with) reads the **same final positions at +70..+120** (e.g. ply-111 FEN `8/k2pb3/P1p2pp1/2P1p2p/1Nb1P2P/3NP3/3K2P1/8 w - - 17 56`: our eval +76 vs SF -402) — we think we're comfortably fine, SF sees the bishop pair dominating. Since our own root eval never drops below -300, **`CONTEMPT` stayed at 30, the "avoid draws" branch — it was never gated off.** It still didn't help: at the exact repeated position SF's own top pick is also our move (Kc3), so nothing better was declined; the search legitimately found no improvement given its miscalibrated read. The repetition is a symptom of the earlier drift (ply 33/49 above plus ~15 smaller -5..-50cp slips through the ending), not a contempt failure.

**Ranked items:**
1. New (not queued): add a bishop-pair / minor-piece-imbalance term in `eval.rs` (`structure`/`evaluate`) — this is the root cause of the 400-500cp miscalibration above; bundle it into the already-drafted pawn-structure pass before shipping. Regression: the FEN above should score negative for White, not +76. Est. **+15-25 Elo**. Test: unit test (2B-vs-2N sample FENs) + gauntlet.
2. The already-drafted (untested) doubled-pawn penalty directly targets the ply-33 fxe3-vs-Rxe3 pattern: it widens the static-eval gap between the two recaptures from 7cp to 52cp in the correct direction on this exact FEN. No reorder — ship/test as planned. Est. **+10-20 Elo**.
3. Contempt magnitude: 30cp is not the bug here (see the companion SF2800-draw report for a case where it's genuinely too small); lower priority than the eval fixes above since our own eval is off by 400+cp in minor-piece endings regardless of the contempt value.
