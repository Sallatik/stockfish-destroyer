# Engine-dev: 2026-09-26T095553_sf2400_win (engine-v5 "Destroyer 0.6.0", white vs SF2400, win by mate, 121 plies)

| ply | our move | best (SF d18) | eval loss | cause |
|---|---|---|---|---|
| 11 | Rb1 | dxc5 | 116 cp | eval blind spot |
| 13 | Bd3 | dxc5 | 149 cp | eval blind spot |
| 53 | Nd4 | Qf4 | 150 cp | shallow depth (same pattern, not independently re-verified) |
| 69 | Bf1 | Be4 | 118 cp | shallow depth (same pattern, not independently re-verified) |
| 81 | a7 | Ra7+ | 174 cp | shallow depth (same pattern, not independently re-verified) |
| 111 | c7 | c7 | "8687 cp" | **not a real mistake** — see below |

**dxc5 (ply 11/13):** replayed both FENs on `bin/destroyer-engine-v5`; `dxc5` never becomes best at *any* depth 1-7 (full-width search, so not an ordering artifact) — a real static-eval gap: `eval::evaluate()` has no term for releasing central pawn tension / hitting the exposed Qb6 with tempo, so a capture that doesn't win material can't outscore quiet development, even fully searched.

**ply 111 anomaly:** `best` equals our move `c7` in evalgame.py's own output, so by definition this cannot be an engine mistake. FEN `5k2/3Q4/2P5/8/3b3P/6P1/5rBK/8 w - - 3 56` is a genuine forced mate, confirmed directly with `bin/stockfish` (depth 24: mate 12; depth 30: mate 7, top move `c6c7`). That mate sits right at the edge of evalgame.py's fixed depth-18 pass and flickers in/out: ply109 finds it (953->9987), it's still there in ply111's "before" (9987), vanishes in ply111's "after" (1300, no mate), then reappears one ply later on Black's own move (112: -1300 -> -9991). This is depth-18 mate-score instability in the analysis oracle (likely amplified by evalgame.py reusing one long-lived Stockfish process/hash table across the whole game's sequential `analyse()` calls), not a defect in our search or eval — c7 was objectively winning either way.

**Ranked items:**
1. Null-move + LMR/aspiration (queued) — covers the ply 53/69/81 pattern (7-8 ply ceiling confirmed twice elsewhere in this batch). Est. **+50-80 Elo**.
2. *(tooling, not engine)* in `arena/evalgame.py::main`, skip the `mistake` flag when `best`'s SAN equals the move played — removes false positives like ply 111 from future reports at no engine cost.

Reorder note: third game in this batch pointing at the depth ceiling — recommend null-move/LMR ahead of another king-safety-v2 attempt in the queue.
