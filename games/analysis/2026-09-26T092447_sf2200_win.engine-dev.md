# Engine-dev analysis: 2026-09-26T092447_sf2200_win (v0.4.0/engine-v3, no TT/book)

## Blunder table
| Ply | Move | Best | Eval loss | Likely cause |
|---|---|---|---|---|
| 35 | g4 | Nxf5 | 217cp | shallow depth / move ordering (missed simple recapture-avoiding tactic, no TT) |
| 41 | Kf3 | Kg1 | 338cp | search horizon on forced-check sequence, not an eval bug (see below) |

## Focus: why Kf3 scored well — taper check
FEN before move 21 (`r3kb1r/1p1b2p1/p3p3/3p1P2/3N3q/P1N1P3/1PPQPKB1/2R2R2 w kq - 3 21`): white has 2N+1B+2R+Q, black has 0N+2B+2R+Q → `phase = 21/24`. So `mg_phase=21, eg_phase=3` — the position is **overwhelmingly middlegame-weighted**, not leaning endgame as hypothesized.

Computed tapered king-PST directly from `eval.rs` tables at this phase:
- g1: mg=24, eg=-24 → tapered = **+18**
- f3: mg=-30, eg=16 → tapered = **-24.25**

The MG table (which dominates at phase 21/24) already strongly prefers g1 over f3 by 42–54cp in white's favor. **The taper is not the culprit** — PeSTO's own PST already ranks Kg1 correctly; it just isn't enough to close a 338cp gap. The real loss comes from the *search*, not the static eval: `negamax` (main.rs) has no check extensions, so the forcing line after 21.Kf3 (…e5, …e4+, repeated king walks) that punishes the king isn't seen until it's inside the fixed depth, while `quiesce` only widens on captures/promotions/in-check-evasions, not on giving-check quiet moves. With no TT yet at this build, effective depth was shallow, so the tactical refutation of Kf3 vs. Kg1 was invisible at the horizon even though the static eval pointed the right way.

## Ranked suggestions (does this game reorder the queue?)
1. **Move "check extensions" ahead of "king safety" in the queue.** This game is a clean example of a check-extension gap costing 338cp with a *correct* PST signal — implement in `negamax` (engine/src/main.rs): extend by 1 ply when `pos.is_check()` before recursing, capped (e.g. total ext ≤ 4/root branch) to avoid blowups. Est. **+40–60 Elo**. Test: replay FEN `r3kb1r/1p1b2p1/p3p3/3p1P2/3N3q/P1N1P3/1PPQPKB1/2R2R2 w kq - 3 21` at fixed 5s and confirm Kg1 is chosen; then gauntlet vs previous build (`arena.gauntlet`).
2. **Add this exact FEN (and the ply-35 Nxf5 FEN) as a fixed search regression test** (e.g. `#[test]` in main.rs or a small `tests/` harness asserting best move at fixed depth). Cheap, prevents silently reintroducing this class of bug once check extensions land. Elo: indirect (regression safety).
3. King safety (already queued) should stay *after* check extensions but keep its priority above killer/history: the PST alone is directionally correct but too weak in magnitude (only ~40cp signal vs. real ~300cp danger); once check extensions land, re-measure whether king-safety terms are still needed before implementing.
