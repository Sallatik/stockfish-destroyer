[project]
name = "stockfish-destroyer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["chess>=1.11"]

[project.scripts]
arena = "arena.play:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["arena"]
